import { createRequire as _createRequire } from 'node:module';
const require = _createRequire(import.meta.url);
import {
  DashboardMenuPluginAction,
  EXTERNAL_PAGE_URL_ERROR,
  ExtensionDataClient,
  VALID_PATH_REGEX,
  componentGenerator,
  createCustomElementFolderValidation,
  createDashboardRouteValidation,
  createServicePluginValidation,
  escapeTsString,
  folderExistsMessage,
  generateAppTools,
  generateCustomElement,
  generateDashboardExternalPage,
  generateDashboardMenuPlugin,
  generateDashboardModal,
  generateDashboardPage,
  generateDashboardPlugin,
  generateDataCollection,
  generateEditorReactComponent,
  generateEmbeddedScript,
  generateEvent,
  generateFunctionLibrary,
  generateServicePlugin,
  generateSitePlugin,
  getBackendAppToolsExtensionsDir,
  getBackendDataCollectionsExtensionsDir,
  getBackendEventExtensionsDir,
  getBackendPluginExtensionsDir,
  getDashboardExternalPageExtensionsDir,
  getDashboardMenuPluginExtensionsDir,
  getDashboardModalExtensionsDir,
  getDashboardPluginExtensionsDir,
  getEcomRequiredApps,
  getExtensionRelativePaths,
  isReservedCustomElementName,
  isValidExternalPageUrl,
  modifyExtensionsEntry,
  normalizeExternalPageUrl,
  pathAvailableValidation,
  require_baseIndexOf,
  require_baseProperty,
  require_baseUnary,
  require_camelCase,
  require_castSlice,
  require_hasUnicode,
  require_nodeUtil,
  require_stringToArray
} from "./chunk-SZM6BLAH.js";
import {
  isStandaloneExtensionsProject
} from "./chunk-FTQ4YIHX.js";
import {
  getContextProviderExtensionPath,
  getContextProviderExtensionsDir,
  getEditorReactComponentExtensionsDir,
  getEmbeddedScriptExtensionsDir,
  getFunctionLibraryExtensionsDir,
  getSitePluginExtensionsDir,
  getSiteWidgetExtensionsDir
} from "./chunk-CJ57RXWR.js";
import {
  ComponentType,
  DevCenterClient
} from "./chunk-X4W6DXKI.js";
import {
  require_baseTrim,
  require_isObject,
  require_toNumber
} from "./chunk-SH75G5RE.js";
import {
  require_baseToString,
  require_kebabCase,
  require_toString
} from "./chunk-JHSYPFGD.js";
import {
  require_baseGetTag,
  require_isObjectLike
} from "./chunk-TODKQSCF.js";
import {
  external_exports
} from "./chunk-SDLCFZRL.js";
import {
  __commonJS,
  __dirname,
  __toESM,
  init_esm_shims
} from "./chunk-EXLZF52D.js";

// ../../node_modules/lodash/_charsEndIndex.js
var require_charsEndIndex = __commonJS({
  "../../node_modules/lodash/_charsEndIndex.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var baseIndexOf = require_baseIndexOf();
    function charsEndIndex(strSymbols, chrSymbols) {
      var index = strSymbols.length;
      while (index-- && baseIndexOf(chrSymbols, strSymbols[index], 0) > -1) {
      }
      return index;
    }
    module.exports = charsEndIndex;
  }
});

// ../../node_modules/lodash/_charsStartIndex.js
var require_charsStartIndex = __commonJS({
  "../../node_modules/lodash/_charsStartIndex.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var baseIndexOf = require_baseIndexOf();
    function charsStartIndex(strSymbols, chrSymbols) {
      var index = -1, length = strSymbols.length;
      while (++index < length && baseIndexOf(chrSymbols, strSymbols[index], 0) > -1) {
      }
      return index;
    }
    module.exports = charsStartIndex;
  }
});

// ../../node_modules/lodash/trim.js
var require_trim = __commonJS({
  "../../node_modules/lodash/trim.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var baseToString = require_baseToString();
    var baseTrim = require_baseTrim();
    var castSlice = require_castSlice();
    var charsEndIndex = require_charsEndIndex();
    var charsStartIndex = require_charsStartIndex();
    var stringToArray = require_stringToArray();
    var toString = require_toString();
    function trim2(string, chars, guard) {
      string = toString(string);
      if (string && (guard || chars === void 0)) {
        return baseTrim(string);
      }
      if (!string || !(chars = baseToString(chars))) {
        return string;
      }
      var strSymbols = stringToArray(string), chrSymbols = stringToArray(chars), start = charsStartIndex(strSymbols, chrSymbols), end = charsEndIndex(strSymbols, chrSymbols) + 1;
      return castSlice(strSymbols, start, end).join("");
    }
    module.exports = trim2;
  }
});

// ../../node_modules/lodash/_baseIsRegExp.js
var require_baseIsRegExp = __commonJS({
  "../../node_modules/lodash/_baseIsRegExp.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var baseGetTag = require_baseGetTag();
    var isObjectLike = require_isObjectLike();
    var regexpTag = "[object RegExp]";
    function baseIsRegExp(value) {
      return isObjectLike(value) && baseGetTag(value) == regexpTag;
    }
    module.exports = baseIsRegExp;
  }
});

// ../../node_modules/lodash/isRegExp.js
var require_isRegExp = __commonJS({
  "../../node_modules/lodash/isRegExp.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var baseIsRegExp = require_baseIsRegExp();
    var baseUnary = require_baseUnary();
    var nodeUtil = require_nodeUtil();
    var nodeIsRegExp = nodeUtil && nodeUtil.isRegExp;
    var isRegExp = nodeIsRegExp ? baseUnary(nodeIsRegExp) : baseIsRegExp;
    module.exports = isRegExp;
  }
});

// ../../node_modules/lodash/_asciiSize.js
var require_asciiSize = __commonJS({
  "../../node_modules/lodash/_asciiSize.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var baseProperty = require_baseProperty();
    var asciiSize = baseProperty("length");
    module.exports = asciiSize;
  }
});

// ../../node_modules/lodash/_unicodeSize.js
var require_unicodeSize = __commonJS({
  "../../node_modules/lodash/_unicodeSize.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var rsAstralRange = "\\ud800-\\udfff";
    var rsComboMarksRange = "\\u0300-\\u036f";
    var reComboHalfMarksRange = "\\ufe20-\\ufe2f";
    var rsComboSymbolsRange = "\\u20d0-\\u20ff";
    var rsComboRange = rsComboMarksRange + reComboHalfMarksRange + rsComboSymbolsRange;
    var rsVarRange = "\\ufe0e\\ufe0f";
    var rsAstral = "[" + rsAstralRange + "]";
    var rsCombo = "[" + rsComboRange + "]";
    var rsFitz = "\\ud83c[\\udffb-\\udfff]";
    var rsModifier = "(?:" + rsCombo + "|" + rsFitz + ")";
    var rsNonAstral = "[^" + rsAstralRange + "]";
    var rsRegional = "(?:\\ud83c[\\udde6-\\uddff]){2}";
    var rsSurrPair = "[\\ud800-\\udbff][\\udc00-\\udfff]";
    var rsZWJ = "\\u200d";
    var reOptMod = rsModifier + "?";
    var rsOptVar = "[" + rsVarRange + "]?";
    var rsOptJoin = "(?:" + rsZWJ + "(?:" + [rsNonAstral, rsRegional, rsSurrPair].join("|") + ")" + rsOptVar + reOptMod + ")*";
    var rsSeq = rsOptVar + reOptMod + rsOptJoin;
    var rsSymbol = "(?:" + [rsNonAstral + rsCombo + "?", rsCombo, rsRegional, rsSurrPair, rsAstral].join("|") + ")";
    var reUnicode = RegExp(rsFitz + "(?=" + rsFitz + ")|" + rsSymbol + rsSeq, "g");
    function unicodeSize(string) {
      var result = reUnicode.lastIndex = 0;
      while (reUnicode.test(string)) {
        ++result;
      }
      return result;
    }
    module.exports = unicodeSize;
  }
});

// ../../node_modules/lodash/_stringSize.js
var require_stringSize = __commonJS({
  "../../node_modules/lodash/_stringSize.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var asciiSize = require_asciiSize();
    var hasUnicode = require_hasUnicode();
    var unicodeSize = require_unicodeSize();
    function stringSize(string) {
      return hasUnicode(string) ? unicodeSize(string) : asciiSize(string);
    }
    module.exports = stringSize;
  }
});

// ../../node_modules/lodash/toFinite.js
var require_toFinite = __commonJS({
  "../../node_modules/lodash/toFinite.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var toNumber = require_toNumber();
    var INFINITY = 1 / 0;
    var MAX_INTEGER = 17976931348623157e292;
    function toFinite(value) {
      if (!value) {
        return value === 0 ? value : 0;
      }
      value = toNumber(value);
      if (value === INFINITY || value === -INFINITY) {
        var sign = value < 0 ? -1 : 1;
        return sign * MAX_INTEGER;
      }
      return value === value ? value : 0;
    }
    module.exports = toFinite;
  }
});

// ../../node_modules/lodash/toInteger.js
var require_toInteger = __commonJS({
  "../../node_modules/lodash/toInteger.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var toFinite = require_toFinite();
    function toInteger(value) {
      var result = toFinite(value), remainder = result % 1;
      return result === result ? remainder ? result - remainder : result : 0;
    }
    module.exports = toInteger;
  }
});

// ../../node_modules/lodash/truncate.js
var require_truncate = __commonJS({
  "../../node_modules/lodash/truncate.js"(exports, module) {
    "use strict";
    init_esm_shims();
    var baseToString = require_baseToString();
    var castSlice = require_castSlice();
    var hasUnicode = require_hasUnicode();
    var isObject = require_isObject();
    var isRegExp = require_isRegExp();
    var stringSize = require_stringSize();
    var stringToArray = require_stringToArray();
    var toInteger = require_toInteger();
    var toString = require_toString();
    var DEFAULT_TRUNC_LENGTH = 30;
    var DEFAULT_TRUNC_OMISSION = "...";
    var reFlags = /\w*$/;
    function truncate2(string, options) {
      var length = DEFAULT_TRUNC_LENGTH, omission = DEFAULT_TRUNC_OMISSION;
      if (isObject(options)) {
        var separator = "separator" in options ? options.separator : separator;
        length = "length" in options ? toInteger(options.length) : length;
        omission = "omission" in options ? baseToString(options.omission) : omission;
      }
      string = toString(string);
      var strLength = string.length;
      if (hasUnicode(string)) {
        var strSymbols = stringToArray(string);
        strLength = strSymbols.length;
      }
      if (length >= strLength) {
        return string;
      }
      var end = length - stringSize(omission);
      if (end < 1) {
        return omission;
      }
      var result = strSymbols ? castSlice(strSymbols, 0, end).join("") : string.slice(0, end);
      if (separator === void 0) {
        return result + omission;
      }
      if (strSymbols) {
        end += result.length - end;
      }
      if (isRegExp(separator)) {
        if (string.slice(end).search(separator)) {
          var match, substring = result;
          if (!separator.global) {
            separator = RegExp(separator.source, toString(reFlags.exec(separator)) + "g");
          }
          separator.lastIndex = 0;
          while (match = separator.exec(substring)) {
            var newEnd = match.index;
          }
          result = result.slice(0, newEnd === void 0 ? end : newEnd);
        }
      } else if (string.indexOf(baseToString(separator), end) != end) {
        var index = result.lastIndexOf(separator);
        if (index > -1) {
          result = result.slice(0, index);
        }
      }
      return result + omission;
    }
    module.exports = truncate2;
  }
});

// ../cli-astro-commands/src/components/GenerateCommand/extensionHandlers.ts
init_esm_shims();

// ../cli-astro-commands/src/components/GenerateCommand/dashboard-page/extension.ts
init_esm_shims();

// ../cli-astro-commands/src/components/GenerateCommand/generate-schema/definitions.ts
init_esm_shims();
function defineField(field) {
  return JSON.stringify(field);
}

// ../cli-astro-commands/src/components/GenerateCommand/dashboard-page/extension.ts
var schema = external_exports.object({
  extensionType: external_exports.literal("DASHBOARD_PAGE"),
  title: external_exports.string().describe(defineField({ title: "Page title", default: "My Custom Page" })),
  route: external_exports.string().regex(VALID_PATH_REGEX).describe(
    defineField({
      title: "Page route",
      description: "Lowercase alphanumeric, hyphens, and slashes. Used as the page URL path and the extension folder name.",
      default: "my-page"
    })
  )
});
var dashboardPage = {
  title: "Dashboard Page",
  description: "Create a custom dashboard page",
  group: "Dashboard",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/dashboard/dashboard-pages/add-a-dashboard-page-extension",
  zodSchema: schema,
  validate: async (projectFolder, data) => {
    const result = createDashboardRouteValidation(projectFolder)(data.route);
    return result.valid ? null : result.reason;
  },
  generate: ({ projectFolder, data }) => generateDashboardPage(projectFolder, data.route, data.title)
};

// ../cli-astro-commands/src/components/GenerateCommand/dashboard-modal/extension.ts
init_esm_shims();

// ../cli-astro-commands/src/components/GenerateCommand/utils/derive-folder.ts
init_esm_shims();
var import_kebabCase = __toESM(require_kebabCase(), 1);
var import_trim = __toESM(require_trim(), 1);
var import_truncate = __toESM(require_truncate(), 1);
var MAX_DERIVED_FOLDER_LENGTH = 50;
var VALID_CUSTOM_ELEMENT_NAME_REGEX = /^[a-z][a-z0-9]*(-[a-z0-9]+)+$/;
function deriveFolder(source) {
  const folder = (0, import_truncate.default)(
    (0, import_trim.default)((0, import_kebabCase.default)(source).replaceAll(/[^a-z0-9]+/g, "-"), "-").replace(
      /^[^a-z]+/,
      ""
    ),
    { length: MAX_DERIVED_FOLDER_LENGTH, separator: "-", omission: "" }
  );
  if (!folder) {
    return void 0;
  }
  return folder;
}
function deriveCustomElementFolder(source, suffix) {
  const folder = deriveFolder(source);
  if (!folder) {
    return void 0;
  }
  const elementFolder = folder.includes("-") && !isReservedCustomElementName(folder) ? folder : `${folder}-${suffix}`;
  return VALID_CUSTOM_ELEMENT_NAME_REGEX.test(elementFolder) ? elementFolder : void 0;
}
function enrichWithDerivedFolder(data, { sourceField }) {
  const source = data[sourceField];
  const folder = data.folder ?? deriveFolder(source);
  if (!folder) {
    throw new Error(
      `Could not derive a folder from ${sourceField} '${source}'. Use a ${sourceField} containing latin letters, or pass 'folder' explicitly.`
    );
  }
  return { ...data, folder };
}
function enrichCustomElementWithDerivedFolder(data, {
  sourceField,
  suffix
}) {
  const source = data[sourceField];
  const folder = data.folder ?? deriveCustomElementFolder(source, suffix);
  if (!folder) {
    throw new Error(
      `Could not derive a folder from ${sourceField} '${source}'. Use a ${sourceField} containing latin letters, or pass 'folder' explicitly.`
    );
  }
  return { ...data, folder };
}

// ../cli-astro-commands/src/components/GenerateCommand/dashboard-modal/extension.ts
var schema2 = external_exports.object({
  extensionType: external_exports.literal("DASHBOARD_MODAL"),
  title: external_exports.string().describe(
    defineField({ title: "Modal Title", default: "My Custom Modal" })
  ),
  folder: external_exports.string().regex(VALID_PATH_REGEX).optional().describe(
    defineField({
      title: "Modal folder",
      description: "Lowercase alphanumeric and hyphens only. Derived from the modal title when omitted."
    })
  )
});
var dashboardModal = {
  title: "Dashboard Modal",
  description: "Create a custom modal dialog for dashboard pages",
  group: "Dashboard",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/dashboard/dashboard-modals/add-a-dashboard-modal-extension",
  zodSchema: schema2,
  enrich: (data) => enrichWithDerivedFolder(data, { sourceField: "title" }),
  validate: async (projectFolder, data) => {
    const isAvailable = pathAvailableValidation(
      getDashboardModalExtensionsDir(projectFolder)
    )(data.folder);
    return isAvailable ? null : folderExistsMessage("modal", data.folder, "title");
  },
  generate: ({ projectFolder, data }) => generateDashboardModal(projectFolder, data.folder, data.title)
};

// ../cli-astro-commands/src/components/GenerateCommand/plugins/dashboard-plugin/extension.ts
init_esm_shims();
function buildSlotIdField(containers) {
  if (containers.length === 0) {
    return external_exports.string().describe(
      defineField({
        title: "Choose which dashboard app to extend",
        description: "Component ID of the back-office extension container to extend. Obtain from the Dev Center."
      })
    );
  }
  const literals = containers.map(
    (c) => external_exports.literal(c.component.componentId).describe(
      defineField({
        title: c.component.data.backOfficeExtensionContainer.slotData?.displayName ?? c.component.name
      })
    )
  );
  const field = literals.length === 1 ? literals[0] : external_exports.union(
    literals
  );
  return field.describe(
    defineField({
      title: "Choose which dashboard app to extend",
      description: "Component ID of the back-office extension container to extend."
    })
  );
}
var dashboardPlugin = {
  title: "Dashboard Plugin",
  description: "Add a UI component to a predefined slot in Wix apps' dashboards",
  group: "Dashboard",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/dashboard/dashboard-plugins/add-a-dashboard-plugin-extension",
  zodSchema: async (httpClient) => {
    const client = new ExtensionDataClient(httpClient);
    const containers = await client.getBackofficeExtensionWidgets();
    return external_exports.object({
      extensionType: external_exports.literal("DASHBOARD_PLUGIN"),
      extendsSlotId: buildSlotIdField(containers),
      title: external_exports.string().describe(
        defineField({
          title: "Plugin title",
          default: "My Custom Dashboard Plugin"
        })
      ),
      folder: external_exports.string().regex(VALID_PATH_REGEX).optional().describe(
        defineField({
          title: "Plugin folder",
          description: "Lowercase alphanumeric and hyphens only. Must be unique within src/dashboard/plugins/. Derived from the plugin title when omitted."
        })
      )
    }).transform((data) => ({
      ...data,
      slotsAppId: containers.find(
        (c) => c.component.componentId === data.extendsSlotId
      )?.appId
    }));
  },
  enrich: (data) => enrichWithDerivedFolder(data, { sourceField: "title" }),
  validate: async (projectFolder, data) => {
    const isAvailable = pathAvailableValidation(
      getDashboardPluginExtensionsDir(projectFolder)
    )(data.folder);
    return isAvailable ? null : folderExistsMessage("plugin", data.folder, "title");
  },
  generate: async ({ projectFolder, data, httpClient }) => {
    const client = new ExtensionDataClient(httpClient);
    const sdkSlotData = data.slotsAppId ? await client.getSdkSlotData({
      slotId: data.extendsSlotId,
      appId: data.slotsAppId
    }).catch(() => {
    }) : void 0;
    return generateDashboardPlugin(
      projectFolder,
      data.extendsSlotId,
      data.folder,
      data.title,
      sdkSlotData?.importData
    );
  }
};

// ../cli-astro-commands/src/components/GenerateCommand/plugins/dashboad-menu-plugin/extension.ts
init_esm_shims();
var actionSchema = external_exports.union([
  external_exports.object({
    pageId: external_exports.string().uuid().describe(defineField({ title: "Page ID" }))
  }).describe(JSON.stringify({ title: "Navigate to page" })),
  external_exports.object({
    modalId: external_exports.string().uuid().describe(defineField({ title: "Modal ID" }))
  }).describe(JSON.stringify({ title: "Open modal" }))
]).describe(
  defineField({
    title: "Action",
    description: "Choose what happens when the menu item is clicked"
  })
);
function buildSlotIdField2(containers) {
  if (containers.length === 0) {
    return external_exports.string().describe(
      defineField({
        title: "Slot to extend",
        description: "Component ID of the back-office menu extension container to extend. Obtain from the Dev Center."
      })
    );
  }
  const literals = containers.map(
    (c) => external_exports.literal(c.component.componentId).describe(
      defineField({
        title: c.component.data.backOfficeExtensionContainer.slotData?.displayName ?? c.component.name
      })
    )
  );
  const field = literals.length === 1 ? literals[0] : external_exports.union(
    literals
  );
  return field.describe(
    defineField({
      title: "Slot to extend",
      description: "Component ID of the back-office menu extension container to extend."
    })
  );
}
var dashboardMenuPlugin = {
  title: "Dashboard Menu Plugin",
  description: "Add a menu item to dashboard navigation and action menus in Wix apps",
  group: "Dashboard",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/dashboard/dashboard-plugins/add-a-dashboard-menu-plugin-extension",
  zodSchema: async (httpClient) => {
    const client = new ExtensionDataClient(httpClient);
    const containers = await client.getBackofficeMenuItems();
    return external_exports.object({
      extensionType: external_exports.literal("DASHBOARD_MENU_PLUGIN"),
      extendsSlotId: buildSlotIdField2(containers),
      action: actionSchema,
      title: external_exports.string().describe(
        defineField({ title: "Menu item title", default: "My Menu Item" })
      ),
      folder: external_exports.string().regex(VALID_PATH_REGEX).optional().describe(
        defineField({
          title: "Plugin folder",
          description: "Lowercase alphanumeric and hyphens only. Must be unique within src/dashboard/menu-plugins/. Derived from the menu item title when omitted."
        })
      )
    });
  },
  enrich: (data) => enrichWithDerivedFolder(data, { sourceField: "title" }),
  validate: async (projectFolder, data) => {
    const isAvailable = pathAvailableValidation(
      getDashboardMenuPluginExtensionsDir(projectFolder)
    )(data.folder);
    return isAvailable ? null : folderExistsMessage("menu plugin", data.folder, "title");
  },
  generate: ({ projectFolder, data }) => {
    const actionType = "pageId" in data.action ? DashboardMenuPluginAction.NavigateToPage() : DashboardMenuPluginAction.OpenModal();
    const action = "pageId" in data.action ? { navigateToPage: { pageId: data.action.pageId } } : { openModal: { componentId: data.action.modalId } };
    return generateDashboardMenuPlugin(
      projectFolder,
      data.extendsSlotId,
      actionType,
      action,
      data.folder,
      data.title
    );
  }
};

// ../cli-astro-commands/src/components/GenerateCommand/event/extension.ts
init_esm_shims();
var schema3 = external_exports.object({
  extensionType: external_exports.literal("EVENT"),
  folder: external_exports.string().regex(VALID_PATH_REGEX).describe(
    defineField({
      title: "Event folder",
      description: "Lowercase alphanumeric and hyphens only"
    })
  )
});
var event = {
  title: "Backend Event",
  description: "Run code in response to a site event",
  group: "Backend",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/backend/events/add-a-backend-event-extension",
  zodSchema: schema3,
  validate: async (projectFolder, data) => {
    const isAvailable = pathAvailableValidation(
      getBackendEventExtensionsDir(projectFolder)
    )(data.folder);
    return isAvailable ? null : "The event folder must be unique, please choose a folder that has not been used yet in your application.";
  },
  generate: ({ projectFolder, data }) => generateEvent(projectFolder, data.folder)
};

// ../cli-astro-commands/src/components/GenerateCommand/service-plugin/extension.ts
init_esm_shims();
var schema4 = external_exports.object({
  extensionType: external_exports.literal("SERVICE_PLUGIN"),
  pluginType: external_exports.union([
    external_exports.literal(ComponentType.ECOM_ADDITIONAL_FEES).describe(defineField({ title: "Ecom Additional Fees" })),
    external_exports.literal(ComponentType.ECOM_SHIPPING_RATES).describe(defineField({ title: "Ecom Shipping Rates" })),
    external_exports.literal(ComponentType.ECOM_DISCOUNTS_TRIGGER).describe(defineField({ title: "Ecom Discount Triggers" })),
    external_exports.literal(ComponentType.ECOM_VALIDATIONS).describe(defineField({ title: "Ecom Validations" })),
    external_exports.literal(ComponentType.ECOM_PAYMENT_SETTINGS).describe(defineField({ title: "Ecom Payment Settings" })),
    external_exports.literal(ComponentType.GIFT_CARDS_PROVIDER).describe(defineField({ title: "Ecom Gift Cards Provider" })),
    external_exports.literal(ComponentType.STAFF_SORTING_PROVIDER).describe(defineField({ title: "Staff Sorting Provider" })),
    external_exports.literal(ComponentType.BOOKING_POLICY_PROVIDER).describe(defineField({ title: "Booking Policy Provider" })),
    external_exports.literal(ComponentType.AVAILABILITY_TIME_SLOTS_CONFIGURATION_PROVIDER).describe(
      defineField({
        title: "Availability Time Slots Configuration Provider"
      })
    ),
    external_exports.literal(ComponentType.BOOKINGS_VALIDATION_PROVIDER).describe(defineField({ title: "Bookings Validation Provider" })),
    external_exports.literal(ComponentType.TOOLS_PROVIDER_CONFIG).describe(defineField({ title: "Tools Provider" })),
    external_exports.literal(ComponentType.BOOKINGS_PRICING_PROVIDER).describe(defineField({ title: "Bookings Pricing Provider" })),
    external_exports.literal(ComponentType.REALTIME_PERMISSIONS_PROVIDER).describe(defineField({ title: "Realtime Permissions Provider" }))
  ]).describe(
    defineField({
      title: "Which service plugin do you want to create?",
      description: "Grouped by business solution. Choose the Wix service this plugin extends."
    })
  ),
  name: external_exports.string().regex(VALID_PATH_REGEX).max(19).describe(
    defineField({
      title: "Plugin name",
      description: "Lowercase alphanumeric and hyphens only."
    })
  )
});
var servicePlugin = {
  title: "Service Plugin",
  description: "Connect to an external service or API",
  group: "Backend",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/backend/service-plugins/add-a-service-plugin-extension",
  zodSchema: schema4,
  validate: async (projectFolder, data) => {
    const result = createServicePluginValidation(
      getBackendPluginExtensionsDir(projectFolder)
    )(data.name);
    return result.valid ? null : result.reason;
  },
  generate: ({ projectFolder, data }) => generateServicePlugin(projectFolder, data.pluginType, data.name)
};

// ../cli-astro-commands/src/components/GenerateCommand/embedded-script/extension.ts
init_esm_shims();
var schema5 = external_exports.object({
  extensionType: external_exports.literal("EMBEDDED_SCRIPT"),
  name: external_exports.string().describe(defineField({ title: "Script name" })),
  folder: external_exports.string().regex(VALID_PATH_REGEX).optional().describe(
    defineField({
      title: "Script folder",
      description: "Lowercase alphanumeric and hyphens only. Must be unique within src/site/embedded-scripts/. Derived from the script name when omitted."
    })
  ),
  scriptType: external_exports.union([
    external_exports.literal("ESSENTIAL").describe(defineField({ title: "Essential" })),
    external_exports.literal("FUNCTIONAL").describe(defineField({ title: "Functional" })),
    external_exports.literal("ANALYTICS").describe(defineField({ title: "Analytics" })),
    external_exports.literal("ADVERTISING").describe(defineField({ title: "Advertising" }))
  ]).describe(
    defineField({
      title: "Script type",
      description: "Determines the consent category of the script."
    })
  ),
  placement: external_exports.union([
    external_exports.literal("HEAD").describe(defineField({ title: "Head" })),
    external_exports.literal("BODY_START").describe(defineField({ title: "Body Start" })),
    external_exports.literal("BODY_END").describe(defineField({ title: "Body End" }))
  ]).describe(
    defineField({
      title: "Placement",
      description: "Where in the page HTML the script is injected."
    })
  )
});
var embeddedScript = {
  title: "Embedded Script",
  description: "Add a custom script to your site pages",
  group: "Site",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/site/embedded-scripts/add-an-embedded-script-extension",
  zodSchema: schema5,
  enrich: (data) => enrichWithDerivedFolder(data, { sourceField: "name" }),
  validate: async (projectFolder, data) => {
    const isAvailable = pathAvailableValidation(
      getEmbeddedScriptExtensionsDir(projectFolder)
    )(data.folder);
    return isAvailable ? null : folderExistsMessage("script", data.folder, "name");
  },
  // scriptType and placement are Zod string literals but generateEmbeddedScript expects TypeScript enums
  generate: ({ projectFolder, data }) => generateEmbeddedScript(
    projectFolder,
    data.folder,
    data.name,
    data.scriptType,
    data.placement
  )
};

// ../cli-astro-commands/src/components/GenerateCommand/custom-element/extension.ts
init_esm_shims();
var schema6 = external_exports.object({
  extensionType: external_exports.literal("CUSTOM_ELEMENT"),
  name: external_exports.string().describe(defineField({ title: "Widget name" })),
  folder: external_exports.string().regex(/^[a-z][a-z0-9]*(-[a-z0-9]+)+$/).optional().describe(
    defineField({
      title: "Widget folder",
      description: "Must be a valid custom element name: lowercase, starts with a letter, contains at least one hyphen. Derived from the widget name when omitted (suffixed with '-element' when needed)."
    })
  )
});
var customElement = {
  title: "Custom Element",
  description: "A site widget built as a custom HTML element",
  group: "Site",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/site/site-widgets/add-a-site-widget-extension",
  zodSchema: schema6,
  enrich: (data) => enrichCustomElementWithDerivedFolder(data, {
    sourceField: "name",
    suffix: "element"
  }),
  validate: async (projectFolder, data) => {
    const dir = getSiteWidgetExtensionsDir(projectFolder);
    if (!pathAvailableValidation(dir)(data.folder)) {
      return folderExistsMessage("widget", data.folder, "name");
    }
    const result = createCustomElementFolderValidation(dir)(data.folder);
    return result.valid ? null : result.reason;
  },
  generate: ({ projectFolder, data }) => generateCustomElement(projectFolder, data.folder, data.name)
};

// ../cli-astro-commands/src/components/GenerateCommand/site-plugin/extension.ts
init_esm_shims();
function buildWidgetPluginSlots(plugins) {
  const slotOptions = plugins.flatMap(
    (wp) => wp.component.data.widgetPlugin.placements.map((p) => ({
      value: `${wp.component.componentId}:${p.slotId}`,
      title: `${wp.component.name} \u2014 ${p.slotId}`
    }))
  );
  return slotOptions.map(
    (opt) => external_exports.literal(opt.value).describe(defineField({ title: opt.title }))
  );
}
function parseSitePluginSlotId(slotId) {
  const colonIndex = slotId.indexOf(":");
  return {
    widgetId: slotId.substring(0, colonIndex),
    slotId: slotId.substring(colonIndex + 1)
  };
}
var sitePlugin = {
  title: "Site Plugin",
  description: "Extend site functionality with a plugin",
  group: "Site",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/site/site-plugins/add-a-site-plugin-extension",
  zodSchema: async (httpClient) => {
    const client = new ExtensionDataClient(httpClient);
    const plugins = await client.getWidgetPlugins();
    const slotLiterals = buildWidgetPluginSlots(plugins);
    return external_exports.object({
      extensionType: external_exports.literal("SITE_PLUGIN"),
      slotId: external_exports.union(slotLiterals).describe(
        defineField({
          title: "Slot",
          description: "The widget slot to extend. Format: componentId:slotId."
        })
      ),
      name: external_exports.string().describe(defineField({ title: "Plugin name" })),
      folder: external_exports.string().regex(/^[a-z][a-z0-9]*(-[a-z0-9]+)+$/).optional().describe(
        defineField({
          title: "Plugin folder",
          description: "Must be a valid custom element name: lowercase, starts with a letter, contains at least one hyphen. Derived from the plugin name when omitted (suffixed with '-plugin' when needed)."
        })
      )
    });
  },
  enrich: (data) => enrichCustomElementWithDerivedFolder(data, {
    sourceField: "name",
    suffix: "plugin"
  }),
  validate: async (projectFolder, data) => {
    const dir = getSitePluginExtensionsDir(projectFolder);
    if (!pathAvailableValidation(dir)(data.folder)) {
      return folderExistsMessage("plugin", data.folder, "name");
    }
    const result = createCustomElementFolderValidation(dir)(data.folder);
    return result.valid ? null : result.reason;
  },
  generate: async ({ projectFolder, appId, data, httpClient }) => {
    const { widgetId, slotId } = parseSitePluginSlotId(data.slotId);
    const plugins = await new ExtensionDataClient(
      httpClient
    ).getWidgetPlugins();
    const widgetPlugin = plugins.find(
      (wp) => wp.component.componentId === widgetId
    );
    const ecomRequiredApps = await getEcomRequiredApps();
    const requireAppId = ecomRequiredApps.appId === widgetPlugin?.appId ? ecomRequiredApps.requiredApps[0].appId : widgetPlugin?.appId;
    const placement = plugins.find((wp) => wp.component.componentId === widgetId)?.component.data.widgetPlugin.placements.find((p) => p.slotId === slotId);
    if (!placement) {
      throw new Error(`Unknown slot: ${data.slotId}`);
    }
    if (requireAppId) {
      await new DevCenterClient(httpClient).addRequiredApp({
        appId,
        appIdToRequire: requireAppId
      });
    }
    return generateSitePlugin(projectFolder, data.folder, data.name, [
      placement
    ]);
  }
};

// ../cli-astro-commands/src/components/GenerateCommand/data-collections/extension.ts
init_esm_shims();
import { existsSync } from "node:fs";
import { join } from "node:path";
var schema7 = external_exports.object({
  extensionType: external_exports.literal("DATA_COLLECTION"),
  collectionName: external_exports.string().regex(/^[A-Za-z0-9_-]{1,36}$/).describe(
    defineField({
      title: "Collection name",
      description: "1-36 characters, letters, numbers, underscores, and hyphens only."
    })
  )
});
var dataCollection = {
  title: "Data Collection",
  description: "Define a collection to store and manage site data",
  group: "Backend",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/backend/data-collections/add-a-data-collection-extension",
  zodSchema: schema7,
  validate: async (projectFolder, data) => {
    const dir = getBackendDataCollectionsExtensionsDir(projectFolder);
    return existsSync(join(dir, `${data.collectionName}.ts`)) ? "Collection name must be unique, please choose a name that has not been used yet in your application." : null;
  },
  generate: ({ projectFolder, data }) => generateDataCollection(projectFolder, data.collectionName)
};

// ../cli-astro-commands/src/components/GenerateCommand/editor-react-component/extension.ts
init_esm_shims();
var schema8 = external_exports.object({
  extensionType: external_exports.literal("EDITOR_REACT_COMPONENT"),
  name: external_exports.string().max(30).describe(
    defineField({ title: "Extension name", default: "e.g., my-component" })
  ),
  folder: external_exports.string().regex(VALID_PATH_REGEX).optional().describe(
    defineField({
      title: "Component folder",
      description: "Lowercase alphanumeric and hyphens only. Derived from the component name when omitted."
    })
  ),
  description: external_exports.string().min(3).max(300).describe(defineField({ title: "Component description" }))
});
var editorReactComponent = {
  title: "Editor React Component",
  description: "Build a reusable UI component with styles",
  group: "Site",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/site/editor-react-components/about-editor-react-components",
  zodSchema: schema8,
  enrich: (data) => enrichWithDerivedFolder(data, { sourceField: "name" }),
  validate: async (projectFolder, data) => {
    const isAvailable = pathAvailableValidation(
      getEditorReactComponentExtensionsDir(projectFolder)
    )(data.folder);
    return isAvailable ? null : folderExistsMessage("component", data.folder, "name");
  },
  generate: ({ projectFolder, model, data }) => generateEditorReactComponent(
    projectFolder,
    data.folder,
    data.name,
    data.description,
    model.config.projectType === "App" ? model.config.codeIdentifier : void 0
  )
};

// ../cli-astro-commands/src/components/GenerateCommand/context-provider/extension.ts
init_esm_shims();

// ../cli-astro-commands/src/components/GenerateCommand/context-provider/generate.ts
init_esm_shims();
var import_camelCase = __toESM(require_camelCase(), 1);
import path from "node:path";
import { randomUUID } from "node:crypto";
var CONTEXT_PROVIDER_TEMPLATE = path.resolve(
  __dirname,
  "../templates/astro/context-provider"
);
async function generateContextProvider(projectFolder, folder, name, type, codeIdentifier) {
  const cwd = getContextProviderExtensionPath(projectFolder, folder);
  const extensionFileName = path.basename(folder);
  const { relativePathToCodeFile, relativePathToConfigFile } = getExtensionRelativePaths({
    cwd,
    projectFolder,
    extensionFileName,
    codeFileType: "tsx"
  });
  const config = {
    id: randomUUID(),
    name: escapeTsString(name),
    type: escapeTsString(codeIdentifier ? `${codeIdentifier}.${type}` : type),
    provider: relativePathToCodeFile
  };
  const isCustomExtensionsProject = await isStandaloneExtensionsProject(projectFolder);
  await modifyExtensionsEntry({
    projectFolder,
    extensionName: (0, import_camelCase.default)(folder),
    relativePath: relativePathToConfigFile,
    isCustomExtensionsProject
  });
  return await componentGenerator({
    template: CONTEXT_PROVIDER_TEMPLATE,
    projectFolder,
    cwd,
    data: {
      builderPackage: isCustomExtensionsProject ? "@wix/custom-extensions/experimental" : "@wix/astro/builders/experimental",
      route: extensionFileName,
      config
    }
  });
}

// ../cli-astro-commands/src/components/GenerateCommand/context-provider/extension.ts
var schema9 = external_exports.object({
  extensionType: external_exports.literal("CONTEXT_PROVIDER"),
  name: external_exports.string().describe(
    defineField({ title: "Extension name", default: "e.g., my-data-source" })
  ),
  folder: external_exports.string().regex(VALID_PATH_REGEX).optional().describe(
    defineField({
      title: "Provider folder",
      description: "Lowercase alphanumeric, hyphens, and slashes. Derived from the provider name when omitted."
    })
  ),
  type: external_exports.string().min(1).describe(
    defineField({
      title: "Provider type",
      description: "The context provider type suffix. The project code identifier is prepended automatically."
    })
  )
});
var contextProvider = {
  title: "Data Source",
  description: "Expose data and logic directly to the UI",
  group: "Site",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/code-model/context-providers/about-context-providers",
  zodSchema: schema9,
  enrich: (data) => enrichWithDerivedFolder(data, { sourceField: "name" }),
  validate: async (projectFolder, data) => {
    const isAvailable = pathAvailableValidation(
      getContextProviderExtensionsDir(projectFolder)
    )(data.folder);
    return isAvailable ? null : folderExistsMessage("context provider", data.folder, "name");
  },
  generate: ({ projectFolder, model, data }) => generateContextProvider(
    projectFolder,
    data.folder,
    data.name,
    data.type,
    model.config.projectType === "App" ? model.config.codeIdentifier : void 0
  )
};

// ../cli-astro-commands/src/components/GenerateCommand/dashboard-external-page/extension.ts
init_esm_shims();
var schema10 = external_exports.object({
  extensionType: external_exports.literal("DASHBOARD_EXTERNAL_PAGE"),
  url: external_exports.string().refine(isValidExternalPageUrl, { message: EXTERNAL_PAGE_URL_ERROR }).transform(normalizeExternalPageUrl).describe(
    defineField({
      title: "External URL",
      description: "The URL of the external page to load"
    })
  ),
  folder: external_exports.string({ required_error: `'folder' must be specified` }).regex(VALID_PATH_REGEX).describe(
    defineField({
      title: "Page folder",
      description: "Lowercase alphanumeric, hyphens, and slashes."
    })
  )
});
var dashboardExternalPage = {
  title: "Dashboard External Page",
  description: "A Dashboard page that loads an external URL",
  group: "Dashboard",
  documentation: "https://dev.wix.com/docs/studio-two/code-your-site/extensions/dashboard/dashboard-external-pages/add-a-dashboard-external-page-extension",
  zodSchema: schema10,
  validate: async (projectFolder, data) => {
    const isAvailable = pathAvailableValidation(
      getDashboardExternalPageExtensionsDir(projectFolder)
    )(data.folder);
    return isAvailable ? null : `The page folder '${data.folder}' already exists. Choose a folder that has not been used yet in your application.`;
  },
  generate: ({ projectFolder, data }) => generateDashboardExternalPage(projectFolder, data.folder, data.url)
};

// ../cli-astro-commands/src/components/GenerateCommand/app-tools/extension.ts
init_esm_shims();
var appToolConfigSchema = external_exports.object({
  methodName: external_exports.string().min(1).max(30).describe(
    defineField({
      title: "Method name",
      description: "Name of the method that implements this tool."
    })
  ),
  description: external_exports.string().min(10).max(1e3).describe(
    defineField({
      title: "Description",
      description: "What this tool does \u2014 used by AI agents to decide when to call it."
    })
  ),
  requestSchema: external_exports.record(external_exports.unknown()).optional().describe(
    defineField({
      title: "Request schema",
      description: "JSON Schema for the tool input parameters."
    })
  ),
  responseSchema: external_exports.record(external_exports.unknown()).optional().describe(
    defineField({
      title: "Response schema",
      description: "JSON Schema for the tool output."
    })
  ),
  activated: external_exports.boolean().optional().describe(
    defineField({
      title: "Activated",
      description: "Whether the tool is active. Defaults to false."
    })
  )
});
var schema11 = external_exports.object({
  extensionType: external_exports.literal("APP_TOOLS"),
  name: external_exports.string().regex(VALID_PATH_REGEX).max(19).describe(
    defineField({
      title: "Extension name",
      description: "Lowercase alphanumeric and hyphens only."
    })
  ),
  tools: external_exports.array(appToolConfigSchema).min(1).max(100).optional().describe(defineField({ title: "Tools" }))
});
var appTools = {
  title: "App Tools",
  description: "A backend extension that provides tools to Wix AI agents",
  group: "Backend",
  zodSchema: schema11,
  validate: async (projectFolder, data) => {
    const dir = getBackendAppToolsExtensionsDir(projectFolder);
    return pathAvailableValidation(dir)(data.name) ? null : "Extension name must be unique, please choose a name that has not been used yet.";
  },
  generate: ({ projectFolder, data }) => generateAppTools(projectFolder, data.name, data.tools)
};

// ../cli-astro-commands/src/components/GenerateCommand/function-library/extension.ts
init_esm_shims();
var schema12 = external_exports.object({
  extensionType: external_exports.literal("FUNCTION_LIBRARY"),
  name: external_exports.string().describe(defineField({ title: "Extension name" })),
  folder: external_exports.string().regex(VALID_PATH_REGEX).optional().describe(
    defineField({
      title: "Extension folder",
      description: "Lowercase alphanumeric and hyphens only. Derived from the extension name when omitted."
    })
  )
});
var functionLibrary = {
  title: "Function Library",
  description: "Write reusable utility functions",
  group: "Site",
  zodSchema: schema12,
  enrich: (data) => enrichWithDerivedFolder(data, { sourceField: "name" }),
  validate: async (projectFolder, data) => {
    const isAvailable = pathAvailableValidation(
      getFunctionLibraryExtensionsDir(projectFolder)
    )(data.folder);
    return isAvailable ? null : folderExistsMessage("function library", data.folder, "name");
  },
  generate: ({ projectFolder, model, data }) => generateFunctionLibrary(
    projectFolder,
    data.folder,
    data.name,
    model.config.projectType === "App" ? model.config.codeIdentifier : void 0
  )
};

// ../cli-astro-commands/src/components/GenerateCommand/extensionHandlers.ts
var extensionHandlers = {
  DASHBOARD_PAGE: dashboardPage,
  DASHBOARD_MODAL: dashboardModal,
  DASHBOARD_PLUGIN: dashboardPlugin,
  DASHBOARD_MENU_PLUGIN: dashboardMenuPlugin,
  DASHBOARD_EXTERNAL_PAGE: dashboardExternalPage,
  EVENT: event,
  SERVICE_PLUGIN: servicePlugin,
  EMBEDDED_SCRIPT: embeddedScript,
  CUSTOM_ELEMENT: customElement,
  SITE_PLUGIN: sitePlugin,
  DATA_COLLECTION: dataCollection,
  EDITOR_REACT_COMPONENT: editorReactComponent,
  CONTEXT_PROVIDER: contextProvider,
  APP_TOOLS: appTools,
  FUNCTION_LIBRARY: functionLibrary
};

export {
  extensionHandlers
};
//# sourceMappingURL=chunk-36DSPSNZ.js.map